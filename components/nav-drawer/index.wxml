<t-drawer 
    style="padding-top: max(32rpx, env(safe-area-inset-bottom));" 
    title="" 
    placement="left" 
    visible="{{visible}}" 
    items="{{sidebar}}" 
    bind:item-click="onClick" 
    bind:close="onClose" 
/>