<t-tab-bar value="{{ value }}" theme="tag" split="{{ false }}" bind:change="onChange">
  <t-tab-bar-item wx:for="{{ list }}" icon="{{ item.icon }}" value="{{ item.value }}">
    {{ item.label }}
  </t-tab-bar-item>
</t-tab-bar>