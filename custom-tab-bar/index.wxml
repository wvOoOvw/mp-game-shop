<t-tab-bar theme="tag" split="{{ false }}" value="{{ value }}" bind:change="onChange">
  <t-tab-bar-item wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}" value="{{ item.value }}">
    {{ item.label }}
  </t-tab-bar-item>
</t-tab-bar>